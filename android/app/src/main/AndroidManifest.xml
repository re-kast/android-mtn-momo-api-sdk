<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:name=".MomoApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.MTNMomoAPI"
        android:windowSoftInputMode="adjustResize"
        tools:ignore="UnusedAttribute,LockedOrientationActivity"
        tools:targetApi="31">
        <activity
            android:name="io.rekast.sdk.sample.views.splash.SplashScreenActivity"
            android:exported="true"
            android:theme="@style/Theme.MTNMomoAPI.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="io.rekast.sdk.sample.views.main.MainActivity"
            android:exported="true"
            android:theme="@style/Theme.MTNMomoAPI.NoActionBar" />
    </application>
</manifest>
